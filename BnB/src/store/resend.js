import { defineStore } from "pinia";

export const useEmailStore = defineStore("email", () => {
  // Store for email-related state can be added here if needed
  // For now, EmailJS is used directly in the Contact component
  return {};
});
